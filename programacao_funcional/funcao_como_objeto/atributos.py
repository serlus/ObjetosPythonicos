def dobro(x):
    """investigar como uma função funciona
    for p in dir(dobro): Assim investigo quais são seus atributos
        print(p)

    Assim posso ver os atributos e operações ligadas a função. 
    __name__: me da o nome da função
    __module__: o modulo onde esta a função
    __code__:
    __code__.co_code: compilação em bit do codigo python gerado pela codigo
    dis: ajuda a interpretar o q faz o código binario
    from dis import dis
    """
    return x * 2